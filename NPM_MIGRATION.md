# Migration from pnpm to npm

**Date:** October 10, 2025  
**Status:** Complete ✅

## Changes Made

### Configuration Files Updated

1. **client/package.json**
   - Removed `packageManager` field
   - Updated `engines` to specify npm instead of pnpm

2. **client/Dockerfile.dev**
   - Removed pnpm installation
   - Changed to use `npm ci` or `npm install`
   - Updated CMD to use `npm run dev`

3. **docker-compose.yml**
   - Removed obsolete `version` field
   - Updated client service command to `npm run dev`

4. **client/.gitignore**
   - Added pnpm-lock.yaml to ignore list
   - Keep package-lock.json tracked

5. **client/.dockerignore**
   - Removed pnpm-debug.log

### Documentation Updated

6. **README.md** (root)
   - All pnpm commands → npm commands
   - Updated prerequisites

7. **client/README.md**
   - All pnpm commands → npm commands
   - Updated quick start
   - Updated all code examples

8. **CONTRIBUTING.md**
   - Updated testing commands

9. **DOCKER_GUIDE.md**
   - Updated client test commands

10. **STORY03_COMPLETION_SUMMARY.md**
    - Updated all references

11. **STORY04_COMPLETION_SUMMARY.md**
    - Updated all references

12. **.github/CODEOWNERS**
    - Changed pnpm-lock.yaml → package-lock.json

## Usage

### Install Dependencies

```bash
cd client
npm install
```

### Run Development Server

```bash
npm run dev
```

### Other Commands

```bash
npm run build          # Build for production
npm run preview        # Preview production build
npm run lint           # Lint code
npm run lint:fix       # Fix linting issues
npm run format         # Format code
npm run format:check   # Check formatting
npm run type-check     # TypeScript type checking
npm test               # Run tests
```

## Docker

The Docker configuration now uses npm:

```bash
# Build client image
docker-compose build client

# Start all services
docker-compose up -d

# View client logs
docker-compose logs -f client
```

## Files to Track

- ✅ `package.json` - Tracked
- ✅ `package-lock.json` - Tracked (generated by npm install)
- ❌ `pnpm-lock.yaml` - Not tracked (ignored)
- ❌ `yarn.lock` - Not tracked (ignored)

---

**Migration Complete** - All references to pnpm have been replaced with npm.

