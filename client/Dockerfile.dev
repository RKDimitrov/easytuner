FROM node:20-alpine

WORKDIR /app

# Install system dependencies (optional; keep minimal for Vite dev)
RUN apk add --no-cache bash

# Default command is overridden by docker-compose, but keep a sensible default
EXPOSE 3000

CMD ["npm", "run", "dev", "--", "--host", "--port", "3000"]
